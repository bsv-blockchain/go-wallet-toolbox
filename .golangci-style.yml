version: "2" # Added in v2: required to specify config version

# options for analysis running
run:
  # exit code when at least one issue was found, default is 1
  issues-exit-code: 1

  # include test files or not, default is true
  tests: true

  # by default isn't set. If set we pass it to "go list -mod={option}". From "go help modules":
  # If invoked with -mod=readonly, the go command is disallowed from the implicit
  # automatic updating of go.mod described above. Instead, it fails when any changes
  # to go.mod are needed. This setting is most useful to check that go.mod does
  # not need updates, such as in a continuous integration and testing system.
  # If invoked with -mod=vendor, the go command assumes that the vendor
  # directory holds the correct copies of dependencies and ignores
  # the dependency descriptions in go.mod.
  # modules-download-mode: readonly|release|vendor

  # Allow multiple parallel golangci-lint instances running.
  # If false (default) - golangci-lint acquires file lock on start.
  allow-parallel-runners: false

# output configuration options
output:
  formats:
    text:
      path: stdout

      # print linter name in the end of issue text, default is true
      print-linter-name: true

      # print lines of code with issue, default is true
      print-issued-lines: true

  # add a prefix to the output file references; default is no prefix
  path-prefix: ""

linters:
  # v1: disable-all: true -> v2: default: none
  # Disable all linters.
  default: none

  # Enable specific linters
  enable:
    - misspell

  settings:
    misspell:
      # Correct spellings using locale preferences for US or UK.
      # Default is to use a neutral variety of English.
      # Setting locale to US will correct the British spelling of 'colour' to 'color'.
      locale: US

      # Previously: `ignore-words`, now renamed to `ignore-rules` in v2
      # Custom dictionary words to ignore
      ignore-rules:
        - bsv
        - bitcoin
        - serialise

  exclusions:
    # Migrated: replaces `issues.exclude-generated` (was set to false → now lax to preserve same behavior)
    generated: lax

    # Migrated: combines exclude-files + exclude-dirs from v1
    paths:
      - .*\.my\.go$
      - lib/bad.go
      - .github
      - .make
      - dist
      - third_party$
      - builtin$
      - examples$

# issue handling rules
issues:
  # Maximum issues count per one linter. Set to 0 to disable. Default is 50.
  max-issues-per-linter: 0

  # Maximum count of issues with the same text. Set to 0 to disable. Default is 3.
  max-same-issues: 0

  # Show only new issues created after git revision `REV`
  new-from-rev: ""

severity:
  # Default severity for all issues.
  # - Code climate: https://docs.codeclimate.com/docs/issues#issue-severity
  # - Checkstyle: https://checkstyle.sourceforge.io/property_types.html#severity
  # - GitHub: https://docs.github.com/en/actions/using-workflows/workflow-commands-for-github-actions#setting-an-error-message
  default: error

  # Optional rules to override severity based on linter or regex match
  rules:
    - linters:
        - dupl
      severity: info

# Added in v2: formatter-specific section
formatters:
  enable:
    - gci

  settings:
    gci:
      # Import sorting sections:
      # - standard: built-in packages
      # - default: unresolved imports
      # - prefix: custom project modules
      sections:
        - standard
        - default
        - prefix(bitcoin-sv/spv-wallet)

  # Mirrors linters.exclusions: formatters now support their own exclusion rules
  exclusions:
    generated: lax
    paths:
      - .*\.my\.go$
      - lib/bad.go
      - .github
      - .make
      - dist
      - third_party$
      - builtin$
      - examples$
